import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF003C90);
  static const secondary = Color(0xFF0F52BA);
  static const background = Color(0xFFF7F9FB);
  static const surface = Color(0xFFFFFFFF); // Cambiado de Colors.white a Color(0xFFFFFFFF)
  static const success = Color(0xFF10B981); // Presente
  static const error = Color(0xFFBA1A1A); // Falta
  static const warning = Color(0xFFEAB308); // Justificado
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const border = Color(0xFFE5E7EB);
}