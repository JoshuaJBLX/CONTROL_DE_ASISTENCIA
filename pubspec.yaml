name: app_control_de_asistencia
description: A Flutter project for attendance control
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.6
  
  # Estado
  provider: ^6.1.1
  
  # Image Picker
  image_picker: ^1.0.5

  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/   # Esto incluye TODOS los archivos en assets/